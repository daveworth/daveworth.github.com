---
import BaseLayout from './BaseLayout.astro';
import BackToHome from '../components/BackToHome.astro';
import GitHubProfileLink from '../components/GitHubProfileLink.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  pubDate: Date;
  description?: string;
  image?: string;
}

const { title, pubDate, description, image } = Astro.props;

const formattedDate = pubDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<BaseLayout title={title} description={description} image={image}>
  <div class="max-w-4xl mx-auto px-4 py-6 sm:py-8 lg:flex lg:gap-8">
    <aside class="lg:w-72 lg:flex-shrink-0 mb-6 sm:mb-8 lg:mb-0">
      <div class="lg:fixed lg:w-72 lg:pr-4">
        <header class="text-center sm:text-center lg:text-left pb-4 sm:pb-6 lg:pb-0 border-b sm:border-b lg:border-b-0 border-gray-200">
          <h1 class="text-xl sm:text-2xl font-bold text-site-heading">{title}</h1>
          <p class="text-xs sm:text-sm text-gray-500 mt-2">{formattedDate}</p>
          <div class="mt-3 sm:mt-4">
            <BackToHome />
          </div>
          <div class="mt-2">
            <GitHubProfileLink />
          </div>
        </header>
      </div>
    </aside>
    <main class="flex-1 max-w-full sm:max-w-xl lg:max-w-xl">
      <article class="prose prose-sm sm:prose-lg max-w-none">
        <slot />
      </article>
      <div class="mt-6 sm:mt-8 p-3 sm:p-4 bg-gray-50 rounded">
        <p class="text-xs sm:text-sm">
          Have something to contribute? Open an
          <a href="https://github.com/daveworth/daveworth.github.com/issues" class="text-site-link hover:underline">Issue</a>
          on <a href="https://github.com" class="text-site-link hover:underline">Github</a> and let's have a chat!
        </p>
      </div>
      <Footer />
    </main>
  </div>
</BaseLayout>
